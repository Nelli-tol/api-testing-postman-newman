{
  "name": "api-testing-postman-newman",
  "version": "1.0.0",
  "private": true,
  "description": "API testing using Postman collections + Newman CLI with HTML reporting and GitHub Actions CI.",
  "scripts": {
    "test": "npm run test:html",
    "test:cli": "newman run collections/reqres_api.postman_collection.json -e collections/reqres_environment.postman_environment.json --reporters cli",
    "test:html": "newman run collections/reqres_api.postman_collection.json -e collections/reqres_environment.postman_environment.json --reporters cli,htmlextra --reporter-htmlextra-export newman-report.html --reporter-htmlextra-title \"ReqRes API Test Report\" --reporter-htmlextra-logs",
    "test:json": "newman run collections/reqres_api.postman_collection.json -e collections/reqres_environment.postman_environment.json --reporters cli,json --reporter-json-export newman-report.json"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}